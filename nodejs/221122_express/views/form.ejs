<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>get 요청</h3>
    <form action="/getForm" method="get" name="getform"> <!--form태그의 name은 선택사항-->
        <input type="text" name="id">
        <input type="password" name="pw">
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="m" id="male"><label for="male">남자</label>
            <!-- label태그는 글자를 클릭해도 자동으로 체크박스가 체크가 됨 -->
            <input type="radio" name="gender" value="f" id="female"><label for="female">여자</label>
        </fieldset>
        <!-- fieldset태그는 그룹을 묶어주는 역할 legend태그는 묶음의 제목 지정 -->

        <fieldset>
            <legend>취미</legend>
            <input type="checkbox" name="habbit" value="drama"> 드라마보기
            <input type="checkbox" name="habbit" value="music"> 음악감상 
            <input type="checkbox" name="habbit" value="health"> 운동하기
            <!-- name속성값에 따라 묶음이 달라짐, name속성값이 같은 것끼리 한 묶음 -->
        </fieldset>
        <button type="submit">제출</button>
    </form>
    <!-- action: 정보를 전송할(보낼) 주소를 적음 -->
    <!-- method: get(보내는 정보가 url에 노출이 됨) or post(보내는 정보를 url에서 확인 불가(보안성상승)) -->

    <h3>post 요청</h3>
    <form action="/postForm" method="post" name="postform"> <!--form태그의 name은 선택사항-->
        <input type="text" name="id" placeholder="아이디" required>
        <input type="password" name="pw" placeholder="비밀번호" required>
        <select name="birthMonth">
            <% for(var i=1; i<13; i++) { %>
                <option value="<%=i%>"><%=i%></option>
            <% } %>
        </select>
        <!-- select태그에 name속성, option태그에 value속성값 넣기 -->
        <textarea name="content" rows="10" cols="50" required></textarea>
        <!-- textarea 크기 조정 row와 cols속성 사용 -->
        <button type="submit">제출</button>
    </form>
    
    <script>
        console.log(document.forms["getform"]) //form태그를 선택하게 함
    </script>
</body>
</html>