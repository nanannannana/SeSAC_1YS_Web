<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>getform</title>
</head>
<body>
    <form action="/getForm" method="get" name="getform">
        이름<input type="text">
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="male" id="m"><label for="m">남자</label>
            <input type="radio" name="gender" value="female" id="f"><label for="f">여자</label>
        </fieldset>
        <fieldset>
            <legend>생년월일</legend>
            <select name="birthYear">
                <% for(var i=2010; i<2023 ; i++) { %>
                    <option value="<%=i%>"><%=i%></option>년
                <% } %>
            </select>
            <select name="birthMonth">
                <% for(var i=1; i<13 ; i++) { %>
                    <option value="<%=i%>"><%=i%></option>월
                <% } %>
            </select>
            <select name="birthDate">
                <% for(var i=1; i<32 ; i++) { %>
                    <option value="<%=i%>"><%=i%></option>일
                <% } %>
            </select>
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="habbit" value="trip" id="t"><label for="t">여행</label>
            <input type="checkbox" name="habbit" value="fashion" id="fa"><label for="fa">패션</label>
            <input type="checkbox" name="habbit" value="food" id="fo"><label for="fo">음식</label>
        </fieldset>
        <button type="submit">회원가입</button>
    </form>
</body>
</html>